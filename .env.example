# Public site URL — required for Open Graph / link preview images (WhatsApp, Discord, etc.)
# Also used by Obsidian sync to convert blog links <-> wikilinks on pull/push.
# Use your production domain, no trailing slash.
NEXT_PUBLIC_SITE_URL=https://your-blog.example.com