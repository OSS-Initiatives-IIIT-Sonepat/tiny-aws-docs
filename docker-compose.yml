services:
  vengeance-blog:
    build:
      context: .
      dockerfile: Dockerfile
    image: vengeance-blog-template:latest
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
